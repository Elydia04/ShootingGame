{
  "build": {
    "command": "cd server && npm install && cd .. && npm install && npm run build",
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "always"
  }
}
