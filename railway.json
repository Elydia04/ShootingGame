{
  "build": {
    "command": "npm install && npm install --prefix server && npm run build",
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node server/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "always"
  }
}
