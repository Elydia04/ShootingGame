app = "shooting-game"
primaryRegion = "iad"
consoleCommand = ""
buildCommand = "npm install --prefix server && npx vite build"
startCommand = "node server/index.js"

[env]
  PORT = "3001"

[http_service]
  internalPort = 3001
  forceHttps = true
  healthCheckPath = "/health"
